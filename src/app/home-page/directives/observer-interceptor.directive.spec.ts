import { ObserverInterceptorDirective } from './observer-interceptor.directive';

describe('ObserverInterceptorDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserverInterceptorDirective();
    expect(directive).toBeTruthy();
  });
});
